# Bound to an IAM role via EKS Pod Identity (or IRSA annotation) that can
# only PutObject/ListBucket on the backup prefix for this instance.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-memory
